# 🏗️ MindMesh Architecture

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                         Browser                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │                    MindMesh App                       │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │              React Frontend                     │  │  │
│  │  │  ┌──────────────────────────────────────────┐  │  │  │
│  │  │  │         UI Components Layer             │  │  │  │
│  │  │  │                                          │  │  │  │
│  │  │  │  ┌──────────┐  ┌─────────────┐         │  │  │  │
│  │  │  │  │  Header  │  │  Dashboard  │         │  │  │  │
│  │  │  │  └──────────┘  └─────────────┘         │  │  │  │
│  │  │  │  ┌──────────┐  ┌─────────────┐         │  │  │  │
│  │  │  │  │  Upload  │  │ Knowledge   │         │  │  │  │
│  │  │  │  │ Section  │  │    Mesh     │         │  │  │  │
│  │  │  │  └──────────┘  └─────────────┘         │  │  │  │
│  │  │  │  ┌──────────┐  ┌─────────────┐         │  │  │  │
│  │  │  │  │Flashcards│  │  Settings   │         │  │  │  │
│  │  │  │  └──────────┘  └─────────────┘         │  │  │  │
│  │  │  └──────────────────────────────────────────┘  │  │  │
│  │  │                      │                          │  │  │
│  │  │                      ▼                          │  │  │
│  │  │  ┌──────────────────────────────────────────┐  │  │  │
│  │  │  │         Services Layer                   │  │  │  │
│  │  │  │                                          │  │  │  │
│  │  │  │  ┌──────────────┐  ┌────────────────┐  │  │  │  │
│  │  │  │  │   Storage    │  │   AI Service   │  │  │  │  │
│  │  │  │  │   Service    │  │                │  │  │  │  │
│  │  │  │  │              │  │  - Process     │  │  │  │  │
│  │  │  │  │  - Save      │  │  - Summarize   │  │  │  │  │
│  │  │  │  │  - Load      │  │  - Extract     │  │  │  │  │
│  │  │  │  │  - Export    │  │  - Generate    │  │  │  │  │
│  │  │  │  │  - Import    │  │                │  │  │  │  │
│  │  │  │  └──────────────┘  └────────────────┘  │  │  │  │
│  │  │  └──────────────────────────────────────────┘  │  │  │
│  │  │           │                    │                │  │  │
│  │  └───────────┼────────────────────┼────────────────┘  │  │
│  │              ▼                    ▼                   │  │
│  │  ┌──────────────────┐  ┌─────────────────────────┐  │  │
│  │  │  localStorage    │  │  External APIs          │  │  │
│  │  │                  │  │  (Optional)             │  │  │
│  │  │  - Materials     │  │                         │  │  │
│  │  │  - Flashcards    │  │  - HuggingFace         │  │  │
│  │  │  - Graph Data    │  │  - OpenAI              │  │  │
│  │  │  - Settings      │  │  - PDF.js (CDN)        │  │  │
│  │  └──────────────────┘  └─────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## Component Hierarchy

```
App
├── Header
│   └── Navigation Items
│       ├── Dashboard
│       ├── Add Material
│       ├── Knowledge Mesh
│       ├── Flashcards
│       └── Settings
│
└── Main Content (Conditional Rendering)
    │
    ├── Dashboard
    │   ├── Stats Cards (3)
    │   ├── Recent Materials List
    │   └── Quick Actions (2)
    │
    ├── UploadSection
    │   ├── Title Input
    │   ├── Tab Selector
    │   │   ├── Text Input Tab
    │   │   └── File Upload Tab
    │   ├── Process Button
    │   └── Results Display
    │       ├── Summary Card
    │       ├── Topics Card
    │       └── Flashcards Preview
    │
    ├── KnowledgeMesh
    │   ├── Graph Controls
    │   ├── D3 SVG Canvas
    │   ├── Legend
    │   └── Node Detail Modal
    │
    ├── Flashcards
    │   ├── Filter Dropdown
    │   ├── Progress Bar
    │   ├── Card Display (Flip Animation)
    │   ├── Navigation Controls
    │   └── Stats Cards (3)
    │
    └── Settings
        ├── AI Provider Config
        ├── Data Management
        │   ├── Load Sample Data
        │   ├── Export Button
        │   ├── Import Button
        │   └── Clear All Button
        └── About Section
```

## Data Flow Diagram

```
┌──────────────┐
│   User       │
│   Action     │
└──────┬───────┘
       │
       ▼
┌──────────────────────┐
│   UI Component       │
│  (Dashboard, etc.)   │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐         ┌─────────────────┐
│   Service Layer      │────────▶│  External APIs  │
│  - AI Service        │         │  (Optional)     │
│  - Storage Service   │         └─────────────────┘
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│   localStorage       │
│  - Read/Write        │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│   State Update       │
│  (React setState)    │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│   UI Re-render       │
│  (Show updated data) │
└──────────────────────┘
```

## Material Processing Pipeline

```
User Upload
    │
    ├─ Text Input ─────┐
    │                  │
    └─ PDF Upload ────▶│ Extract Text
                       │
                       ▼
              ┌────────────────┐
              │  AI Service    │
              │  Process Text  │
              └────────┬───────┘
                       │
          ┌────────────┼────────────┐
          │            │            │
          ▼            ▼            ▼
    Extract       Generate     Generate
    Topics        Summary      Flashcards
          │            │            │
          └────────────┼────────────┘
                       │
                       ▼
              ┌────────────────┐
              │  Create        │
              │  Material      │
              │  Object        │
              └────────┬───────┘
                       │
          ┌────────────┼────────────┐
          │            │            │
          ▼            ▼            ▼
    Update         Update       Update
    Materials    Flashcards   Graph Data
          │            │            │
          └────────────┼────────────┘
                       │
                       ▼
              ┌────────────────┐
              │  Save to       │
              │  localStorage  │
              └────────┬───────┘
                       │
                       ▼
              ┌────────────────┐
              │  Update UI     │
              │  Show Results  │
              └────────────────┘
```

## Knowledge Mesh Generation

```
Material Added
    │
    ▼
Extract Topics
    │
    ├─ Main Topic ────────▶ Create Main Node
    │
    └─ Subtopics ─────────▶ Create Subtopic Nodes
                               │
                               ▼
                         Link to Main Node
                               │
                               ▼
                    ┌──────────────────────┐
                    │  Compare with        │
                    │  Existing Nodes      │
                    │  (Keyword Matching)  │
                    └──────────┬───────────┘
                               │
                ┌──────────────┼──────────────┐
                │                             │
                ▼                             ▼
        Match Found                    No Match
        Create Link                    Skip
                │
                ▼
        ┌────────────────┐
        │  Update Graph  │
        │  - Add Nodes   │
        │  - Add Links   │
        └────────┬───────┘
                 │
                 ▼
        ┌────────────────┐
        │  Save to       │
        │  localStorage  │
        └────────┬───────┘
                 │
                 ▼
        ┌────────────────┐
        │  Render D3     │
        │  Force Graph   │
        └────────────────┘
```

## State Management Flow

```
┌─────────────────────────────────────────────┐
│            App Component (Root)             │
│                                             │
│  State:                                     │
│  - activeView: string                       │
│  - materials: Material[]                    │
│  - flashcards: Flashcard[]                  │
│  - graphData: { nodes, links }              │
│                                             │
│  Effects:                                   │
│  - useEffect(() => loadFromStorage())       │
│                                             │
│  Handlers:                                  │
│  - handleMaterialAdded(newMaterial)         │
│  - handleDeleteMaterial(materialId)         │
│  - updateGraphData(graphData, newMaterial)  │
└────────────┬────────────────────────────────┘
             │
             ├── Props Down ──────────────────┐
             │                                │
   ┌─────────▼─────────┐         ┌───────────▼─────────┐
   │    Dashboard      │         │   UploadSection     │
   │                   │         │                     │
   │ Props:            │         │ Props:              │
   │ - materials       │         │ - onMaterialAdded   │
   │ - flashcards      │         │                     │
   │ - graphData       │         │ Handlers:           │
   │ - onDelete        │         │ - handleProcess()   │
   │ - onNavigate      │         │   └─▶ calls parent │
   └───────────────────┘         └─────────────────────┘
             │                                │
             └── Events Up ───────────────────┘
```

## File Organization

```
src/
├── components/              # UI Components
│   ├── Header.jsx          # Navigation
│   ├── Dashboard.jsx       # Overview
│   ├── UploadSection.jsx   # Content input
│   ├── KnowledgeMesh.jsx   # D3 graph
│   ├── Flashcards.jsx      # Study mode
│   └── Settings.jsx        # Config
│
├── services/               # Business Logic
│   ├── storage.js         # localStorage CRUD
│   └── aiService.js       # AI processing
│
├── utils/                 # Utilities
│   └── sampleData.js     # Demo content
│
├── App.jsx               # Root component
├── main.jsx             # React entry
└── index.css           # Global styles
```

## Technology Stack Layers

```
┌───────────────────────────────────────────┐
│           Presentation Layer              │
│  - React Components                       │
│  - TailwindCSS                           │
│  - Lucide Icons                          │
│  - D3.js Visualizations                  │
└─────────────────┬─────────────────────────┘
                  │
┌─────────────────▼─────────────────────────┐
│           Business Logic Layer            │
│  - AI Service (Processing)                │
│  - Storage Service (CRUD)                 │
│  - Graph Algorithm (Connections)          │
└─────────────────┬─────────────────────────┘
                  │
┌─────────────────▼─────────────────────────┐
│           Data Persistence Layer          │
│  - localStorage API                       │
│  - JSON serialization                     │
└─────────────────┬─────────────────────────┘
                  │
┌─────────────────▼─────────────────────────┐
│           External Services (Optional)    │
│  - HuggingFace API                       │
│  - OpenAI API                            │
│  - PDF.js Library                        │
└───────────────────────────────────────────┘
```

## Request/Response Flow Example

### Adding New Material

```
1. User fills form in UploadSection
   └─▶ Text input or PDF file

2. User clicks "Generate Summary & Flashcards"
   └─▶ handleProcess() called

3. Service Layer Processing
   ├─▶ aiService.extractPDFText() (if PDF)
   ├─▶ aiService.processText()
   │   ├─▶ generateSummary()
   │   ├─▶ extractTopics()
   │   ├─▶ generateFlashcards()
   │   └─▶ generateQuiz()
   └─▶ Returns Material object

4. Pass to Parent (App.jsx)
   └─▶ onMaterialAdded(newMaterial)

5. Update State in App
   ├─▶ Add to materials array
   ├─▶ Add flashcards to flashcards array
   └─▶ Update graph data

6. Persist to Storage
   ├─▶ storageService.saveMaterials()
   ├─▶ storageService.saveFlashcards()
   └─▶ storageService.saveGraphData()

7. React Re-renders
   └─▶ UI shows new material
```

## Build Process

```
Source Code (src/)
       │
       ▼
   Vite Build Tool
       │
       ├─▶ React JSX → JavaScript
       ├─▶ TailwindCSS → Purged CSS
       ├─▶ ES Modules → Bundled
       └─▶ Assets → Optimized
       │
       ▼
   dist/ (Production Build)
       │
       ├─▶ index.html
       ├─▶ assets/
       │   ├─▶ main.js (bundled)
       │   └─▶ main.css (purged)
       └─▶ Static files
```

## Security Architecture

```
┌──────────────────────────────────────────┐
│          Browser Sandbox                 │
│  ┌────────────────────────────────────┐  │
│  │       MindMesh App                 │  │
│  │  ┌──────────────────────────────┐  │  │
│  │  │    localStorage (Private)    │  │  │
│  │  │  - Materials                 │  │  │
│  │  │  - Flashcards                │  │  │
│  │  │  - Settings                  │  │  │
│  │  │  - API Keys                  │  │  │
│  │  └──────────────────────────────┘  │  │
│  │                                     │  │
│  │  Optional External Calls:           │  │
│  │  └─▶ User Initiated Only           │  │
│  │      └─▶ HTTPS Only                │  │
│  └────────────────────────────────────┘  │
│                                          │
│  No Tracking • No Analytics             │
│  No Auto External Requests              │
└──────────────────────────────────────────┘
```

## Performance Optimization Points

```
1. Component Level
   ├─▶ Conditional rendering (active view only)
   ├─▶ React.memo (future enhancement)
   └─▶ useMemo/useCallback (future enhancement)

2. Data Level
   ├─▶ localStorage caching
   ├─▶ Lazy loading (can add)
   └─▶ Pagination (can add)

3. Build Level
   ├─▶ Vite code splitting
   ├─▶ TailwindCSS purging
   ├─▶ Asset minification
   └─▶ Tree shaking

4. Rendering Level
   ├─▶ D3 force simulation optimization
   ├─▶ CSS animations (GPU accelerated)
   └─▶ Debounced inputs (can add)
```

## Deployment Architecture

```
Development:
   localhost:3000 ─▶ Vite Dev Server

Production:
   Source Code
       │
       ▼
   npm run build
       │
       ▼
   dist/ folder
       │
       ▼
   Static Hosting
   ├─▶ Netlify
   ├─▶ Vercel
   ├─▶ GitHub Pages
   └─▶ Any CDN
```

---

**This architecture supports:**
- ✅ Scalability (can handle 100s of materials)
- ✅ Maintainability (clear separation of concerns)
- ✅ Extensibility (easy to add features)
- ✅ Performance (optimized rendering)
- ✅ Security (local-first, no leaks)
- ✅ Offline Support (works without internet)

MindMesh v1.0.0 | Clean, Modern, Scalable Architecture
